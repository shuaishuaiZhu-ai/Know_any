# Session Retrospective: 2026-04-15 13:45

## Summary
Reverted cmd.c from V7 (candidate-driven bitmask dispatch) back to V6 (linear scan dispatch) in the CP user scheduling module. The revert preserved one V7 bug fix (bad-packet drain in cmd_handle_packet default branch) and debug code cleanup from the working tree, while removing all V7 scheduling artifacts (rr_start, candidate, pending_mask, CTZ helpers).

## Errors Encountered
- Bash heredoc failed when attempting to write the entire cmd.c content through a `<< 'ENDOFFILE'` heredoc -- the C source contained characters that interfered with heredoc processing. Resolution: used the local Write tool to create the file, then SCP'd it to the remote server. This proved more reliable than any SSH-based content transfer for whole-file replacement.

## Patterns Used
- **Selective version revert**: rather than surgically removing V7 changes line by line, took V6 baseline (commit 7e3fa00) as the base and selectively merged in two improvements: (1) V7's cmd_handle_packet default branch bad-packet drain, and (2) working tree debug code cleanup. This produced a clean result with minimal risk of V7 residuals.
- **Write + SCP for whole-file transfer**: bypassed all SSH heredoc escaping issues by constructing the file locally with the Write tool and transferring via SCP.
- **Plan mode workflow**: user triggered plan mode via "pm" text command; followed EnterPlanMode -> explore -> clarify -> plan -> ExitPlanMode -> execute flow.
- **Grep-based residual verification**: after revert, searched for V7-specific identifiers (rr_start, candidate, pending_mask, cmd_ctz) to confirm no V7 artifacts remained.
- **Build verification**: ran full build after revert to confirm compilation.

## User Preferences Observed
- Prefers explicit scope clarification before executing ("only revert cmd.c, keep ib.c/ib.h unchanged").
- Comfortable with mixed Chinese/English communication.
- Values build verification as final acceptance gate.

## Improvements Identified
- The Write + SCP pattern should be documented as "Tier 0" in the existing ssh-remote-file-editing learning, since it avoids all escaping pitfalls for whole-file operations.

## Learnings Created
- Updated: `.claude/learnings/patterns/ssh-remote-file-editing.md` -- added Tier 0 (Write + SCP) approach
- New: `.claude/learnings/patterns/selective-version-revert.md` -- base + cherry-pick strategy for clean version rollbacks
